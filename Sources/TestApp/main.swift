// main.swift removed @main to avoid duplicate entry points.
// Use Playgrounds or create a single App entry in your Xcode project.

import SwiftUI

struct TestApp_Reference: View {
    var body: some View { ContentView() }
}
